name: Check final
on:
  workflow_dispatch:
  push:
    branches: ["claude/**"]
    paths: [tool/check_final.py, .github/workflows/check-final.yml]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 tool/check_final.py
