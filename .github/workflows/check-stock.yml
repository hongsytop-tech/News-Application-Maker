name: Check stock
on:
  workflow_dispatch:
  push:
    branches: ["claude/**"]
    paths: [tool/check_stock.py, .github/workflows/check-stock.yml]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 tool/check_stock.py
