name: Deploy Web (GitHub Pages)

on:
  push:
    branches:
      - main
      - "claude/**"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Create .env from secrets
        run: |
          cat > .env <<EOF
          SUPABASE_URL=${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}
          CRAWL_PROXY_URL=${{ secrets.CRAWL_PROXY_URL }}
          EOF

      - run: flutter pub get

      - name: Build web
        # base-href must match the Pages sub-path (the repo name).
        # pwa-strategy=none disables the service worker so redeploys never
        # serve a stale main.dart.js. BUILD_ID is compiled in so the running
        # app can compare itself against the deployed version-info.json.
        run: |
          flutter build web --release --source-maps --pwa-strategy none \
            --base-href "/${{ github.event.repository.name }}/" \
            --dart-define=BUILD_ID=${{ github.sha }}
          printf '{"build":"%s","time":"%s"}\n' \
            "${{ github.sha }}" "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            > build/web/version-info.json

      - name: Cache-bust entry scripts
        # GitHub Pages serves main.dart.js with a 10-min HTTP cache and the
        # filename never changes, so a reload after a redeploy can keep running
        # the stale bundle (and thus the old BUILD_ID). Append a per-deploy
        # query to the entry scripts so each deploy has unique, uncacheable URLs.
        run: |
          SHA=${{ github.sha }}
          sed -i "s#flutter_bootstrap.js#flutter_bootstrap.js?v=${SHA}#g" build/web/index.html
          sed -i "s#main.dart.js#main.dart.js?v=${SHA}#g" build/web/index.html
          if [ -f build/web/flutter_bootstrap.js ]; then
            sed -i "s#\"main.dart.js\"#\"main.dart.js?v=${SHA}\"#g" build/web/flutter_bootstrap.js
          fi

      - uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
