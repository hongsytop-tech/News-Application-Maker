name: Check naver
on:
  workflow_dispatch:
  push:
    branches: ["claude/**"]
    paths: [tool/check_naver.py, .github/workflows/check-naver.yml]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 tool/check_naver.py
