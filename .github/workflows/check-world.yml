name: Check world
on:
  workflow_dispatch:
  push:
    branches: ["claude/**"]
    paths: [tool/check_world.py, .github/workflows/check-world.yml]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 tool/check_world.py
